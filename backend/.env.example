# Demo: không cần database — dùng JSON file
# Copy file này thành .env (optional, defaults OK cho demo)

PORT=5000
NODE_ENV=development

# JWT secret — đổi thành chuỗi ngẫu nhiên khi deploy thật
JWT_SECRET=demo_secret_doi_khi_deploy_that
JWT_EXPIRES_IN=7d

# CORS
FRONTEND_URL=http://localhost:5173
